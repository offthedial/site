import React from "react"
import Layout from "src/components/Layout"

const Loading = () => (
  <Layout>
    <div class="hero">
      <h1>Loading...</h1>
    </div>
  </Layout>
)

export default Loading
